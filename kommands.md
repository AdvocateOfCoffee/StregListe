```bash
$ pip3/pip install virtualenv
$ which python3
$ virtualenv -p "stien til python" venv
$ source venv/bin/activate
$ pip install django
$ django-admin startproject RKGit
$ cd RKGit

$ ./manage.py startapp stregListe

$ ./manage.py migrate

$ ./manage.py runserver

```
